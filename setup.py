from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description="A recommendation model recommending items based on what currently in a user's cart",
    author='ShinSiang Choong',
)
